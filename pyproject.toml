[project]
name = "tmtabor-site"
version = "0.1.0"
description = "Personal site for Thorin Tabor, built with Pelican."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pelican[markdown]>=4.12.0",
]
